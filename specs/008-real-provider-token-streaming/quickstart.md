# Quickstart: Real Provider Token Streaming

```powershell
npm.cmd ci --ignore-scripts
node --test tests/providers/streaming.test.js
node --test
```

No live provider calls are required for this slice.
