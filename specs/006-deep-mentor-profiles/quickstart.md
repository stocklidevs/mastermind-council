# Quickstart: Deep Mentor Profiles

```powershell
npm.cmd ci --ignore-scripts
node --test
npm.cmd run serve
```

Open `http://localhost:4173`, go to Menu, and edit mentor identity fields in the Mentors tab.
